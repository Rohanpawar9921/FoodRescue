services:
  - type: web
    name: foodrescue
    runtime: docker
    plan: free
    buildCommand: echo "No build needed"
    startCommand: php -S 0.0.0.0:$PORT -t /opt/render/project/src
    envVars:
      - key: PORT
        value: 10000
